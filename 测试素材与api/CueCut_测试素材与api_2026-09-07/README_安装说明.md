# 安装说明

目标目录：`F:\CCPJ\CueCut\测试素材与api`

由于当前 ChatGPT 运行环境不能直接写入你的 Windows F 盘，请：

1. 下载并解压本压缩包。
2. 直接把解压后的内容复制到目标目录；或
3. 在 PowerShell 中运行 `install_to_F_drive.ps1`，脚本只执行本地文件复制，不访问网络。

素材包本身不依赖 PowerShell；该脚本仅为安装辅助。
