# WI-020 Handoff｜R06 License / IP Compliance

Role/WI：`R06 / WI-020`
建议状态：`READY_FOR_REVIEW`

## 执行摘要

已独立核对 Motion Primitives 与 Magic UI 的来源 URL、MIT/copyright notice、9 个选用源文件、随包 modified 状态、正式 adapter mapping 预留和 motion-runtime egress 边界。两份原始 notice 均保留，canonical copy 与 `_import` 原件 SHA-256/字节一致；没有新增下载、依赖、CDN、在线字体或 motion-runtime 网络调用。

`Modified=False` 的含义是“相对于 WI-015 提供的本地 zip entry 未被导入流程改写”。pack metadata 没有 upstream commit/tag，因此没有把当前 upstream `main` 当作本地文件的精确版本证明；其中 Motion Primitives 当前 notice 与随包 notice 内容不同但仍报告 MIT，随包 notice 予以保留。

## 变更文件

- `docs/THIRD_PARTY_MOTIONS.md`：更新为 WI-020 canonical source/license ledger，加入上游路径/树 blob、notice hashes、selected/reference 分层、R01 `adapterId` mapping 预留和远程依赖边界。
- `docs/context/WI-020_CONTEXT.md`：补充本次 provenance、mapping、网络范围和并行协作记录。
- `docs/evidence/WI-020.md`：记录读取输入、逐文件 hash、notice 证据、GitHub corroboration、命令结果和测试边界。
- `docs/handoffs/WI-020.md`：本交接。

未修改：`src/motions/licenses/**`（只读核验）、`src/motions/_import/**`、TS/TSX 生产实现、`package.json`、`pnpm-lock.yaml`、secret、media。

## License / source evidence

- Local pack：`src/motions/CueCut2_TalkingHead_Effects_Pack_v0.1.zip`
- Pack SHA-256：`E30C8258FC14265269184C35FC6F8B6851F68AECFB35AB7586B9640C541115D0`
- Archive/extraction：`16` file entries / `16` extracted files；`all_entry_files_equal=True`
- Motion Primitives：GitHub repository [ibelick/motion-primitives](https://github.com/ibelick/motion-primitives)，MIT，`Copyright (c) 2024 ibelick`；notice original/copy `E99C79B4...A65C2A18`，1064 bytes，byte-equal。
- Magic UI：GitHub repository [magicuidesign/magicui](https://github.com/magicuidesign/magicui)，MIT，`Copyright (c) Magic UI`；notice original/copy `0147B842...27B34BA`，1060 bytes，byte-equal。
- Selected files：TextMorph/TextRoll/TextScramble/TextShimmer/AnimatedNumber/AnimatedGroup from Motion Primitives; AnimatedShinyText/NumberTicker/AnimatedList from Magic UI. Full relative paths and hashes are in `docs/evidence/WI-020.md`.

## Formal adapter mapping reservation

Reserved internal keys are: `text-morph`, `text-roll`, `text-scramble`, `text-shimmer`, `animated-shiny-text`, `animated-number`, `number-ticker`, `animated-list`, `animated-group`.

这些 key 只是 R01 handoff mapping，不是本 WI 创建的实现或最终公开名称。`AnimatedList` 必须作为 `ListMotion` primitive 供 `BulletList`/`Steps`/`Checklist`/`FeatureList`/`Tips` 消费，不可直接冒充最终 `Checklist`；十个 PRD Motion Layer preset 另行归入 CueCut motion layer，不新增第三方 source row。

## Commands / results

- `Get-FileHash` + archive stream comparison：pack hash、16/16 count、all entries equal。
- Per-file `Get-FileHash`：9 个 selected source hashes recorded in `docs/evidence/WI-020.md`。
- License source/copy hash and byte comparison：两份 `byte_equal=True`。
- `gh api repos/ibelick/motion-primitives` / `gh api repos/magicuidesign/magicui`：两者 `default_branch=main`、SPDX `MIT`。
- `gh api .../git/trees/main?recursive=1`：9 个 selected upstream paths present；Git blob IDs recorded in the ledger。
- Production motion egress scan：无 runtime-network/CDN/online-font marker；仅保留 R01 registry provenance HTTPS strings。
- `package.json` / `pnpm-lock.yaml` marker scan：`NO_MATCHES`。
- 按 agent-reach 收尾规范尝试 `agent-reach check-update`，但本机命令不可用；未安装或更新任何工具。

本 WI 不运行 renderer/UI/export runtime tests；静态/IP evidence 与 runtime tests 分开记录。

## 删除 / 保留 / 迁移

- 删除：`0`
- 迁移：`0`
- 保留：两份 canonical MIT notices、两份 `_import` 原始 notices、9 个 selected source files 及其来源证据；它们仍是 reference-only，正式 adapter 修改需由 R01 补充 modified/provenance 记录。

## 未决风险与下一步

1. `SOURCE_METADATA.json` 没有 upstream commit/tag；如发布审核要求精确上游版本，需由项目 owner 选择并记录一个明确 revision，再复核文件/notice 对应关系。
2. R01 的 `src/motions` 树在本次只读审计期间发生并行变化；R90 应在 R01 停止编辑后重跑最终静态 egress/registry/license reference 检查。R06 未覆盖或改写 R01 文件。
3. 应用现有 `/api/generate-effects` 与 Alibaba provider 网络路径不属于 motion-runtime 新增依赖；不得将本交接解读为全仓库离线保证。

Evidence：`docs/evidence/WI-020.md`；canonical ledger：`docs/THIRD_PARTY_MOTIONS.md`。

最终交接：`READY_FOR_REVIEW`。
