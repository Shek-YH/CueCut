# WI-015 Handoff｜R02 Migration / Import / Cleanup

Role/WI：`R02 / WI-015`

建议状态：`READY_FOR_REVIEW`

## 执行摘要

已完成指定 zip 的安全解压、来源/语义/集成资料登记、MIT license 复制和静态引用扫描摘要。所有旧 effect/motion 均保守 `KEEP`，没有删除、迁移或生产源码改动。R00 已补跑基线测试、编译门禁和引用计数；本 handoff 不把后续视觉/Renderer/Export 验证冒充已完成。

## 变更文件

- 导入：`src/motions/_import/CueCut2_TalkingHead_Effects_Pack_v0.1/**`（16 files）
- License：`src/motions/licenses/motion-primitives-LICENCE.md`、`src/motions/licenses/magicui-LICENSE.md`
- 审计/计划：`docs/effect-migration/EFFECT_LIBRARY_AUDIT.md`、`OLD_EFFECT_DELETE_PLAN.md`、`NEW_EFFECT_IMPORT_PLAN.md`
- 来源台账：`docs/THIRD_PARTY_MOTIONS.md`
- 证据与交接：`docs/evidence/WI-015.md`、本文件

未修改：`src/motions/registry.ts`、`src/motions/runtime.ts`、`src/effects/**`、`src/editor/**`、`src/render/**`、`src/export/**`、`src/project/**`、package 文件及用户媒体。

## 命令结果

- zip SHA-256：`E30C8258FC14265269184C35FC6F8B6851F68AECFB35AB7586B9640C541115D0`
- 安全校验：16 entries，全部位于预期根目录，无绝对路径/`..` 路径。
- 解压：目标目录存在，文件数 16。
- license：两份 source/copy SHA-256 完全一致，`byte_equal=True`。
- 引用扫描摘要：`effectId=19 files`、`motionId=14`、`registry=37`、`import=96`、`project=70`、`timeline=33`、`renderer=30`、`export=85`、`preset=9`。
- R00 follow-up：`pnpm test --run` = 36 files passed / 1 skipped；`pnpm lint` 与 `pnpm build` = exit 0；`pnpm workflow:check` = PASS。
- `tsconfig.app.json` 的具体配置为 `exclude: ["src/server", "src/motions/_import"]`，用于隔离仅作来源证据的第三方源码。

## 删除/保留/迁移

- 删除：`0`
- 迁移：`0`
- 保留：13 registry candidates、3 concrete `fx-*` instances、现有 legacy motion IDs。
- 理由：registry candidates 仍由 `generationRoute` 进入生产候选索引；concrete instances 和 ring variants 被 fixture/UI/Timeline/Renderer/tests 引用；无充分无引用证据即不删除。

## 后续风险（不阻塞 WI-015 review）

1. 新包源码依赖 `motion/react`、`@/lib/utils`、Tailwind，需 R01 后续 adapter 评估；本 WI 没有引入这些依赖。
2. 视觉质量、deterministic renderer、Timeline/Export parity 尚未验证，属于后续 WI 范围。

## 下一步建议

R90 应独立确认上述证据、LICENSE 字节一致性、旧 effect KEEP 决策与工作流门禁；通过后 R01/R06 可继续 adapter 与 IP compliance 工作。

本状态仅为 `READY_FOR_REVIEW`，不是 `VERIFIED` 或 `ACCEPTED`。
