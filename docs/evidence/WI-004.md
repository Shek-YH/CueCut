# WI-004 Evidence — Effect Registry + Canvas + Inspector

**Status:** READY_FOR_REVIEW  
**Owner:** R02/R05  
**Verifier:** R07 (pending)  
**Gate:** G3/G4 local synthetic/browser

## Scope

Implemented a typed Effect Registry with the 10 supplied MIT candidate references plus three CueCut-owned numeric variants, canonical Canvas cards, selection, normalized Canvas drag/resize, protected Video Layer 0 and Special-first/Common Inspector structure.

## Commands

- pnpm test --run tests/effects/registry.test.ts tests/app/navigation.test.tsx — passed.
- pnpm exec playwright test -g "drags a Canvas|preserves the prototype layout" — passed.

## UI Fidelity Review status

Prototype Mapping is recorded in the Work Item. Layout and component evidence exists at the three required desktop sizes; independent verifier comparison is still pending. No UI redesign or Overlay Studio source was used.

## License / egress

Supplied SVG references are MIT with provenance; no new binary/font/audio asset was copied. No external request occurred.
