# WI-009 Evidence — CueCut Director

**Status:** READY_FOR_REVIEW for local contract; real structured-output conformance needs changes  
**Owner:** R03  
**Verifier:** R07 (pending)  
**Provider:** Alibaba Bailian qwen-plus, OpenAI-compatible endpoint

## Scope

Implemented bounded candidate retrieval, DirectorInput construction, one-call guard, local candidate validation/fallback and service-level schema fallback. Each independent Generate gets one provider boundary; invalid output does not trigger another provider call.

## Local commands

- pnpm test --run tests/director/*.test.ts — passed in the full suite.
- pnpm test --run tests/director/bailianProvider.test.ts — passed.
- Real smoke evidence: docs/evidence/WI-009-real-bailian-2026-09-08.json.
- No live provider call is made by the application/browser bundle.

## Real RT-04 smoke test

The user-authorized smoke test used the locally configured Alibaba Bailian API key without printing or persisting it. The input contained synthetic Project/SRT/candidate context only; neither supplied video was uploaded.

- Initial request: sent, but the PowerShell wrapper failed while indexing a nullable response field; no safe result summary was captured.
- Explicit failure recovery request: HTTP 200 from qwen-plus; request ID and usage are recorded in WI-009-real-bailian-2026-09-08.json.
- Response had choices and valid JSON; top-level schema marker was cuecut.composition/1.
- Local candidate validation failed because at least one generated Effect/Motion/SFX ID did not match the bounded candidate set.
- No automatic second LLM repair was used. The second request was an explicitly recorded diagnostic recovery for this smoke test, not a normal product Generate chain.

## Strict JSON Schema model experiment

The independently selected qwen3.7-plus model was tested once with strict JSON Schema. Bailian returned HTTP 403 with AllocationQuota.FreeTierOnly, so the configured allocation cannot call that model. No automatic model switch or additional retry was made.

The local candidate sanitizer now preserves valid model-generated content/layout/timing while replacing only invalid Effect/Motion/SFX IDs from the bounded capability index.

## Security / data egress

- Secret status: configured locally; value omitted from all output/evidence.
- Destination: dashscope.aliyuncs.com compatible-mode endpoint.
- Data sent: synthetic project metadata, one synthetic SRT segment and candidate names/IDs.
- Video/audio: not sent.
- Cost/quota: one initial smoke request plus one explicit recovery request; provider billing/quota should be checked by the account owner.
