# WI-013 Evidence — Independent E2E Verification

**Status:** IN_PROGRESS  
**Owner:** R07  
**Verifier:** Product Owner/user or separately designated verifier  
**Gate:** G6/G7

## Current evidence

- Browser E2E: local vertical-slice scenarios pass, including required viewports, real supplied 16:9/9:16 MP4 metadata import, playback-preserving timeline seek, SRT seek, independent Timeline/Canvas drag and resize, Effect Lab Cancel/Apply/Undo, the synthetic video-to-SRT-to-Director-to-Workspace flow, and eight local implementation screenshots.
- Unit/component suite: 36 test files, 81 tests passed and 1 explicitly gated real ASR test skipped in the latest fresh run; build and TypeScript lint exit 0; Playwright 11/11 passed in the latest fresh run.
- The current context is also the implementation context; this is not an independent verifier claim.

## RT-01 result

Real host/browser test passed for supplied jj.mp4: H.264 1920×1080 30fps 222.1s. File stayed local as a browser object URL. This is not evidence for RT-02/RT-03/RT-04/RT-08/RT-09.

## RT-02 result

Real host/browser test passed for supplied ComfyUI_00001_qguot_1787042165.mp4: H.264 1080×1920 30fps 62.648s. Canvas switched to 9:16, normalized cards remained rendered, and safe-zone remained visible. File stayed local as a browser object URL.

## RT-03 result

Real ASR passed with the user-authorized Alibaba Bailian `qwen-audio-3.0-asr-flash` request. The audio-only request returned HTTP 200 and a timestamped result; redacted metadata is in `docs/evidence/WI-008-real-bailian-2026-09-08.json`. This context is not an independent verifier claim.

The one-click browser flow is verified with a controlled local response, and a separate user-authorized real full-flow run is recorded in `WI-014`. The real run reached both provider stages, accepted the Director result after local timing clamp, and did not use fallback; this context is not an independent verifier claim.

## Not verified

- Real LLM provider one-call request.
- Full video export.
- Transparent MOV in CapCut/Jianying.
- Product Owner acceptance.

Host FFmpeg Alpha smoke is separately passed for a synthetic 1-second RGBA source; downstream editor compatibility remains blocked.

## Next

A separate verifier should inspect the prototype comparison screenshots and repeat RT-01 through RT-10, marking synthetic and real evidence separately.
