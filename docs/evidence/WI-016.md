# Evidence｜WI-016

状态：`READY_FOR_REVIEW`

## Scope

- Owner：`R01 Motion Architecture / Registry`
- Verifier：`R90`（待独立复核）
- Golden Path：`GP-03`
- 本次只实现 adapter contract、motion/effect registry、参数归一化和本地确定性 frame API。
- 未修改 `_import/**`、`licenses/**`、editor、render、export、project/schema、package/lock 或 secret/media。

## Source / dependency checks

- 已完整读取 `docs/context/WI-016_CONTEXT.md`、`docs/evidence/WI-015-VERIFIER.md`、`docs/context/WI-015_VERIFIER_CONTEXT.md` 和 PRD §8–11。
- WI-015 verifier evidence 为 `PASS`；本 WI 只引用其已记录的本地来源与 MIT notice 路径。
- 新增 production adapter 未引入 `motion/react`、`framer-motion`、Tailwind、`@/lib/utils`、fetch、axios 或其他网络运行时依赖；上游 URL 仅作为 registry provenance metadata。

## Implemented contract

- `src/motions/format.ts`：common/text/number/list 参数类型、默认值、边界归一化和确定性数字格式化。
- `src/motions/adapters.ts`：9 个内容 adapter（5 text、2 number、AnimatedList、AnimatedGroup）以及 10 个 motion-layer adapter；`evaluateAtFrame({ adapterId, frame, fps, role, props })` 返回 `MotionRenderModel`。
- `src/motions/registry.ts`：19 个正式能力定义，字段包含唯一 `id`、`displayName`、`category`、`semanticTags`、`useCases`、`avoidCases`、`defaultProps`、画幅、timing/layout capabilities、source/license reference；原有 12 个 legacy motion ID 仍可查找，其中 `fade` 复用同一唯一 ID 的正式 motion-layer 定义。
- `src/motions/runtime.ts`：保留旧 `evaluateMotion` API，补充 10 个 motion-layer profile、legacy 行为兼容和 `evaluateMotionAtFrame`/`evaluateMotionFrame`。
- `src/effects/registry.ts`：保留原有 effect family/variant，并将 19 个正式 motion adapter 以 `text`、`number`、`list`、`motion-layer` family 接入；正式项带默认参数、能力、来源和 license 引用。

## TDD evidence

先写 `tests/motions/adapters.test.ts`，再运行：

```text
pnpm test --run tests/motions/adapters.test.ts
RED：3 tests failed；缺失正式 19 项索引、effect registry 接入和 bounce frame 行为。
```

之后实现最小 contract，并补充 frame-based adapter RED→GREEN 循环：

```text
pnpm test --run tests/motions/adapters.test.ts
GREEN：4 tests passed。
```

## Commands / results

```text
pnpm test --run tests/motions tests/effects
PASS：4 files、8 tests

pnpm lint
PASS：exit 0

pnpm build
PASS：exit 0；Vite 输出既有 extensionless import warning，不阻塞构建

pnpm workflow:check
PASS：WORKFLOW_CHECK: PASS；IN_PROGRESS: WI-016, WI-020

rg -n -S "motion/react|framer-motion|@/lib/utils|tailwindcss|fetch\(|axios" \
  src/motions/adapters.ts src/motions/format.ts src/motions/registry.ts \
  src/motions/runtime.ts src/effects/registry.ts
PASS：无命中（rg exit 1 表示零命中）
```

完整套件也已运行：

```text
pnpm test --run
36 test files passed、1 skipped；85 tests passed、1 skipped；1 governance test failed。
```

唯一失败为既有 `tests/governance/workflow.test.ts` 仍断言 `IN_PROGRESS: WI-015`，而当前并行 ledger 的真实输出是 `IN_PROGRESS: WI-016, WI-020`。该测试及 governance 文件不在 WI-016 写入边界内，未修改；不构成本 WI motion/effect focused contract 的失败。

## Open review items

- R90 需独立复核正式 registry 的数量、字段、license/source provenance、legacy ID 和 focused/full evidence。
- WI-017 继续负责 DOM/Canvas shared renderer、中文/英文文本测量、9:16/16:9 和 30fps visual/local render parity；本 Evidence 不把本地 model contract 当作 renderer/visual acceptance。
- 全量 governance 测试的旧状态断言需要由对应治理 owner 处理，本 WI 不越界修复。

本文件仅建议 `READY_FOR_REVIEW`，不是 `VERIFIED` 或 `ACCEPTED`。
