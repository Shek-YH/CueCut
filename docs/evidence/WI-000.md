# WI-000 Evidence — Project Bootstrap

**Status:** IN_PROGRESS  
**Owner:** R00  
**Verifier:** R07 (pending independent review)  
**Date:** 2026-09-08

## Scope

- In: root confirmation, local Git initialization, project ignore/editor/runtime rules, package baseline, governance directories, Docker decision.
- Out: remote Git, push, deployment, native shell, paid providers, feature acceptance.

## Resource readiness

- Synthetic/bootstrap: READY.
- Real media: RT-01/RT-02/RT-03/RT-07 READY; RT-09 BLOCKED.
- Secrets: the supplied local .env was read only to authorize the user-requested Bailian ASR call; the key value was never printed or recorded.

## Commands and current evidence

| Command | Exit code | Result |
|---|---:|---|
| git rev-parse --is-inside-work-tree | 128 | Expected: no repository existed before bootstrap |
| node --version | 0 | v24.11.0 |
| npm --version | 0 | 11.6.1 |
| pnpm --version | 0 | 11.19.0 |
| docker --version | command not found | Docker unavailable; mode none |
| ffmpeg --version | 0 | Host FFmpeg available |
| ffprobe --version | 0 | Host FFprobe available |
| pnpm build | 0 | Vite production build passed for bootstrap shell |
| pnpm test --run | 0 | 1 test file, 1 test passed |
| pnpm lint | 0 | TypeScript no-emit check passed |

Latest verification: pnpm test --run = 81 tests passed, 1 explicitly gated real ASR test skipped; pnpm build = 0; pnpm lint = 0; pnpm test:e2e = 11 passed. Real ASR and the real video-to-SRT-to-Director-to-Workspace flow returned HTTP 200; the final Director result passed after local timing clamp.

The first build attempt exited 2 because the Vitest test configuration was typed through the plain Vite config. It was corrected by importing defineConfig from vitest/config; the fresh build/test run above then exited 0.

## Files

Governance files are listed in the Work Item and docs/governance/PROJECT_DISCOVERY.md. No external media or secret values were copied.

## License / data egress

No new runtime dependency was added. One user-authorized external audio call was made to Alibaba Bailian; only redacted metadata is recorded. Supplied SVG packages are MIT with per-package license/provenance files; they remain under the supplied test-material directory until a registry import task copies them with notices.

## Blockers and next

- Baseline build/test evidence will be appended after the minimal package shell is created.
- Independent verifier must review the bootstrap artifacts before status can advance.
