# WI-005 Evidence — Motion Registry + Motion Runtime

**Status:** READY_FOR_REVIEW  
**Owner:** R02/R04  
**Verifier:** R07 (pending)

## Scope

Local registry and deterministic runtime cover fade, scale, spring, pop, directional, rotation and exit presets. Enter and exit remain separate fields. The Effect Lab preview uses local state and does not call a provider.

## Commands

- pnpm test --run tests/motions/registry.test.ts tests/motions/runtime.test.ts — passed.

## Limitations

Renderer fidelity and performance benchmarking are not complete. Real export remains under WI-011.

