# WI-003 Evidence — PlaybackClock + Video + Timeline Core

**Status:** READY_FOR_REVIEW  
**Owner:** R02/R04  
**Verifier:** R07 (pending)  
**Gate:** G3/G4 local + RT-01 host integration

## Scope

Implemented local PlaybackClock, local video object URL lifecycle, canonical media metadata, timeline time math, SRT seek, zoom, playhead scrub, playback-preserving timeline seek, and independent Effect clip move/trim. No full export or real 9:16 acceptance is claimed.

## Commands

- pnpm test --run tests/playback/clock.test.ts tests/editor/timeline/timeMath.test.ts tests/media/video.test.ts — passed.
- pnpm exec playwright test -g "seeks from SRT|drags a Canvas|resizes a Canvas" — passed.
- pnpm exec playwright test -g "seeks on the timeline" — passed while native video remained playing.
- pnpm exec playwright test -g "imports the supplied local 16:9 MP4" — passed.
- pnpm test:e2e — 9 tests passed in the latest fresh run.
- Playback regression: Space toggles the native video to playing and imported video remains unmuted by default.

## RT-01 real evidence

- Input: local supplied MP4 at F:/CCPJ/CueCut3/测试素材与api/jj.mp4.
- ffprobe: H.264, 1920×1080, 30fps, 222.1s.
- Chrome file input created a local blob URL; video readyState became greater than 0.
- Canonical Project duration became 222.10s and PlaybackClock duration was updated to avoid a 30s clamp.
- No upload or network request was used.

## Evidence

- Effect clip drag changed only Effect timing; Playhead style remained unchanged.
- Canvas card drag and resize changed only normalized layout; Playhead style remained unchanged.
- The supplied real 16:9 MP4 metadata path updates both Project duration and PlaybackClock duration.
- The supplied real 9:16 MP4 path updates aspect ratio to 9:16 and keeps Canvas safe-zone visible; portrait evidence is screenshots/08-real-portrait-9x16.png.
- User-provided path mapping: requested nested path was absent; the only matching local asset was F:/CCPJ/CueCut3/测试素材与api/ComfyUI_00001_qguot_1787042165.mp4.
- Screenshots are under docs/evidence/screenshots/.
- Secret status: not configured/read.
