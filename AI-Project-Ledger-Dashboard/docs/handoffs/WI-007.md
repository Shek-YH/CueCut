# WI-007 Handoff — R06 QA / Security / Dogfood

Status: `BLOCKED`

Verification: `NOT VERIFIED`

Acceptance: `NOT ACCEPTED`

## Handoff summary

This execution is handed off as `BLOCKED`. Existing QA/full tests passed, but the user stopped before the real dogfood server/browser flow could be started. No real dogfood spec was created or run, and no production or self-project ledger files were changed.

The prior WI-007 fixture/security checks remain historical evidence. WI-010's independent `PASS` resolves the prior 20-vs-200 Activity finding; it does not provide the missing real local browser evidence for this execution.

## Commands and results

| Command/check | Result |
| --- | --- |
| `pnpm test --run tests/qa` | PASS — 7/7 |
| `pnpm test` | PASS — 69/69 |
| `pnpm test:e2e` | PASS — 1/1 mock-only E2E |
| `pnpm dev:all` and browser `http://127.0.0.1:4173` | NOT RUN; intentionally stopped before long wait |
| Real dogfood spec | NOT CREATED / NOT RUN |
| `pnpm lint` | NOT RUN in this execution |
| `pnpm build` | NOT RUN in this execution |
| Port/process cleanup | CLEAR — no 4173/3100 listener or matching `dev:all` process |

## Blocking items

- Required real local E2E is missing: start `pnpm dev:all`, load the real self project `projectId=ai-project-ledger-dashboard-v1`, and verify the live browser flow.
- Required live mutation is missing: atomically update one real `IN_PROGRESS` task to `COMPLETED`, update `project.updatedAt`, `runtime.lastWriteAt`, and append an ordered `events.jsonl` event; measure write-to-green UI latency and require `<=1000ms`.
- Required malformed JSON/LKG flow is missing: corrupt `tasks.json`, assert the UI warning and retained LKG, then restore the legal ledger and verify restoration.
- Fresh `pnpm lint` and `pnpm build` were not run in this execution.

## Next reviewer action

R90 should run the missing real dogfood flow with a bounded timeout, then run `pnpm test --run tests/qa`, `pnpm test`, `pnpm lint`, `pnpm build`, `pnpm test:e2e`, and the real dogfood spec. Inspect only the allowed-path diff and do not assign `VERIFIED` or `ACCEPTED` from this handoff.
