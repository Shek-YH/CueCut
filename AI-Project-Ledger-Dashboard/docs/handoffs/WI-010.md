# WI-010 Handoff｜Activity recentEvents Default 200

Status: `READY_FOR_REVIEW`

Owner: R02  
Verifier: R90

## Execution identity

- Work item: WI-010
- Owner executionRef: `dce1960e-948a-46ff-81b5-7026190be7cb`
- Started at: `2026-09-08T18:25:09-04:00`

## Delivered

- `server/ledger/store.ts`: changed the default Activity window from `events.slice(-20)` to `events.slice(-200)`.
- `tests/ledger/store.test.ts`: added regression coverage for 205 events, preserving the latest-200 order, and for returning all events when fewer than 200 exist.

The `slice(-200)` behavior preserves source order, returns fewer than 200 events unchanged when the ledger is smaller, and leaves snapshot validation, LKG fallback, and the existing API shape unchanged.

## TDD evidence

### RED

Command:

```text
pnpm exec vitest run tests/ledger/store.test.ts -t "keeps the latest 200 events in order and all events when fewer exist"
```

Exit code: `1`. The new test failed as intended with `expected ... length 200 but got 20` at the `recentEvents` length assertion.

### GREEN

Command:

```text
pnpm exec vitest run tests/ledger/store.test.ts
```

Exit code: `0`. One test file and 5 tests passed, including existing LKG behavior.

## Verification summary

| Check | Exit | Result |
| --- | ---: | --- |
| Focused `pnpm exec vitest run tests/ledger/store.test.ts` | 0 | 1 file / 5 tests passed |
| Full `pnpm test` | 0 | 20 files / 69 tests passed |
| `pnpm lint` | 0 | TypeScript no-emit check passed |
| `pnpm build` | 0 | TypeScript and Vite production build passed |

No files under `web/`, `Skill/`, `.ai-ledger/`, or `packages/ledger-schema/` were changed.

## Review boundary

This handoff is `READY_FOR_REVIEW` only. R90 should independently review the recent-events contract and rerun the focused/full gates. This handoff does not assign `VERIFIED` or `ACCEPTED`.
