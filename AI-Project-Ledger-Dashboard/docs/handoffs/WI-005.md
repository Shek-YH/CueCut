# WI-005 Handoff

Result: READY_FOR_REVIEW

## Handoff summary

R03 implemented the Dashboard UX / React slice against the WI-002/WI-003 aggregate snapshot contract. The browser consumes the single snapshot endpoint and listens for SSE snapshot/error frames. It does not read project files or mutate task state. The current fix also deduplicates the in-flight snapshot request under React StrictMode and wires Artifacts `Open Parent Folder` to WI-009's secure backend action.

## R03 fix execution

- Fix executionRef: `01a082e8-cfcc-7c80-ba91-41876b3b0b9a`
- `web/App.tsx` shares the aggregate snapshot request by projectId under StrictMode, aborts it after the final consumer unmounts, and renders per-artifact opening/success/error feedback.
- `web/data.ts` calls `POST /api/projects/:projectId/artifacts/open` with `{ path: relativeArtifactPath }` and surfaces backend error payloads.
- `tests/ui/dashboard.test.ts` covers the full StrictMode mount/cancellation path and artifact request/result states.
- `tests/e2e/dashboard.spec.ts` counts one aggregate snapshot request and validates the artifact-open POST body in a real browser.

TDD and fresh verification are recorded in `docs/evidence/WI-005.md`. The fix is ready for R90 re-review; this handoff remains `READY_FOR_REVIEW` and is not a `VERIFIED` or `ACCEPTED` decision.

## Review targets

- `web/App.tsx`: view routing, task tree/filtering, status semantics, read-only boundary.
- `web/data.ts`: aggregate snapshot fetch and SSE subscription.
- `web/styles.css`: dashboard layout, fixed status tokens, 4px task status bar.
- `tests/ui/dashboard.test.ts`: Overview, filters, blocker split, activity/roles/artifacts/settings coverage.
- `tests/ui/data.test.ts`: aggregate endpoint and structured SSE error coverage.
- `tests/e2e/dashboard.spec.ts`: browser contract test; currently passes with local Playwright Chromium.

## Verification handoff

- Focused: `pnpm test:focused -- --reporter=dot` — 28/28 passed.
- UI: `pnpm test --run tests/ui` — 9/9 passed.
- Full: `pnpm test` — 61/61 passed.
- Lint: `pnpm lint` — passed.
- Build: `pnpm build` — passed.
- Playwright E2E: `pnpm test:e2e` — 1/1 passed with Chromium running; observed one aggregate snapshot request and validated artifact-open request payload.

Independent verifier R90 should review the diff and evidence before any later verification or acceptance state change.
