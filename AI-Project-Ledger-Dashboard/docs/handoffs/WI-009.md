# WI-009 Handoff｜Secure Artifact Open-Parent-Folder Action

Status: `READY_FOR_REVIEW`

Owner: R02  
Verifier: R90

## Delivered

- `server/api/index.ts`: secure `POST /api/projects/:projectId/artifacts/open` route with registered-project lookup, JSON validation, injectable `artifactOpener`, Windows Explorer opener, and explicit non-Windows `501 PLATFORM_UNAVAILABLE` response.
- `server/security/paths.ts`: artifact paths must be relative; traversal segments and absolute/drive-rooted paths are rejected before containment resolution. Existing sensitive-name and project-root containment checks remain enforced.
- `tests/api/artifact-open.test.ts`: 5 focused API contract tests using an injected opener; Explorer is never launched.
- `tests/security/security.test.ts`: traversal and absolute-path regression coverage.
- `docs/evidence/WI-009.md`: RED/GREEN and verification record.

## Guardrails exercised

- Unknown project IDs are rejected before request path resolution or opener invocation.
- The opener is called only after the relative path has passed sensitivity and project-root containment checks.
- The endpoint returns path metadata only; it does not read or serve artifact contents.
- No task, ledger, registry, watcher, migration, schema, web, or `.ai-ledger` files were changed.

## Verification summary

- Focused: 2 files / 9 tests passed.
- Full: `pnpm test` — 19 files / 61 tests passed.
- Build: `pnpm build` — passed.
- Workspace lint: blocked by pre-existing out-of-scope UI errors in `web/App.tsx` and `tests/ui/dashboard.test.ts`; no WI-009 source error was reported.

## Next action

R90 should independently review WI-009. The UI owner should resolve the unrelated lint errors before overall workspace acceptance. This handoff makes no `VERIFIED` or `ACCEPTED` claim.
