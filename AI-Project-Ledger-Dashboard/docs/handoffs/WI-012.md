# WI-012 Handoff｜Settings Add Project Folder UI

Status: `READY_FOR_REVIEW`

Owner: R03  
Verifier: R90

## Delivered

- Added an accessible Settings `Project root folder` input that submits only `{ rootPath }` through `POST /api/projects`.
- Added typed handling for WI-011 `existing`, `migrated`, and `initialized` results, including registered-project feedback, sanitized warning counts, and stable error messages.
- After success, refreshes `GET /api/projects`, switches to the returned stable project id, loads its aggregate snapshot, and attaches the existing SSE subscription lifecycle.
- Preserved the read-only boundary: no browser project-file reads and no task status/progress/role/session write entry points.
- Added focused UI/data coverage and a real bounded Playwright flow using `pnpm dev:all` plus a temporary empty fixture; no mocked project API was used as final browser evidence.

## TDD and verification

- RED: UI/data and real browser tests failed before the add helper/input existed.
- GREEN: `pnpm test --run tests/ui --reporter=verbose` → 2 files / 13 tests passed.
- `pnpm test:focused -- --reporter=dot` → 7 files / 32 tests passed.
- `pnpm test -- --reporter=dot` → 21 files / 79 tests passed.
- `pnpm lint` → exit 0.
- `pnpm build` → exit 0; Vite transformed 26 modules.
- `pnpm test:e2e` → exit 0; 2/2 Chromium tests passed against the real localhost API.

## Execution identity and scope

- Owner executionRef: `01a08389-6249-7d33-9df4-14e415442539`
- Changed implementation/test files: `web/App.tsx`, `web/data.ts`, `web/styles.css`, `tests/ui/data.test.ts`, `tests/ui/dashboard.test.ts`, `tests/e2e/dashboard.spec.ts`, `tests/e2e/playwright.config.ts`.
- Added WI-012 plan/evidence/handoff docs only. No server, schema, `.ai-ledger`, or global Skill file was changed.

R90 review is requested. This handoff does not mark WI-012 `VERIFIED` or `ACCEPTED`.
