# WI-004 Handoff｜Legacy Markdown Migration / Project Bootstrap

Status: `READY_FOR_REVIEW`

Owner: R04  
Verifier: R90  
Dependency: WI-001

Owner executionRef: `f89eb63c-c1d2-42a9-af67-9cabdd1b700b`

## Delivered

- `server/migration/`: conservative legacy detection, status parsing, project ID derivation, migration, and empty-ledger bootstrap. The finite status mapping includes `正在开发`, `进行中`, `阻塞`, and `等待用户`; phrase matching requires an explicit title-start boundary.
- `server/registry/`: Dashboard-owned project registry with deduplicated add and config-only remove.
- `tests/migration/`: RED/GREEN coverage for detection, status mapping, conservative phrase boundaries, confidence/warnings, unchanged Markdown, skeleton initialization, existing-ledger precedence, and registry behavior.
- `fixtures/project-legacy-md/`: representative legacy Markdown input plus unrelated Markdown.

## Guardrails exercised

- `.ai-ledger` is treated as authoritative; migration does not inspect or overwrite legacy files when it exists.
- Migration reads only directly detected legacy Markdown files.
- Source Markdown is not deleted, moved, or rewritten.
- Machine output uses the seven WI-001 files and validates against the shared schemas.
- Registry removal never touches the registered project path.

## Verification evidence

- TDD RED: focused migration suite failed exactly the two new regression tests before the parser fix.
- TDD GREEN: focused migration suite: 4 files / 13 tests passed.
- Full suite: 15 files / 44 tests passed on a fresh rerun.
- TypeScript lint and build exited successfully.

The first full-suite invocation saw a transient API assertion mismatch (expected 3 events, received 4); the API test passed in isolation and the fresh full rerun passed. No API source or test file was changed.

## Next owner action

R90 should independently reverify the corrected `正在开发` mapping and conservative status phrase boundary, including LOW-confidence warning retention for `未进行中的旧条目`.

Handoff remains `READY_FOR_REVIEW`; no `VERIFIED` or `ACCEPTED` claim is made here.
