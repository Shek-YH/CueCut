# WI-002 Handoff

Status: READY_FOR_REVIEW

Verification: NOT VERIFIED

Acceptance: NOT ACCEPTED

## Handoff summary

WI-002 implements the backend ledger snapshot store and security boundaries in the requested paths. The public entry points are:

- `server/ledger/store.ts`: `LedgerStore`, `loadLedgerSnapshot`, `buildTaskTree`, and snapshot/result types.
- `server/security/paths.ts`: `ALLOWED_LEDGER_FILES`, `SECRET_DENYLIST`, `isSensitivePath`, `isAllowedLedgerFile`, and `resolveContainedArtifactPath`.

The store consumes WI-001 schema and pure aggregation contracts. A valid load returns the aggregate project snapshot; an invalid subsequent load returns the per-root Last Known Good snapshot plus an `INVALID_LEDGER` warning.

## Verification handoff

- WI-002 focused tests: 2 suites / 7 tests passed.
- Existing focused tests: 5 suites / 19 tests passed.
- Full tests: 11 suites / 36 tests passed.
- Lint: passed.
- Build: passed.
- Independent R90 verification remains required.

## Scope guard

R02 changed only `server/ledger/**`, `server/security/**`, `tests/ledger/**`, `tests/security/**`, and these WI-002 evidence/handoff documents. No changes were made to `server/registry`, `server/watcher`, `server/api`, `server/migration`, `web`, `packages/ledger-schema`, or `package.json`.

Next: dispatch R90 for independent review of the implementation, security boundaries, TDD evidence, and full-test result.
