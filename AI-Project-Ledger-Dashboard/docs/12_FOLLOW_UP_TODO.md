# Follow-up TODO

- Agent Board adapter / shared SDK (V2+).
- Optional WebSocket/task editing only after conflict-control design.
- Virtualized Activity list beyond the V1 10,000-event fixture.
- Cross-platform native “open parent folder” implementations.
- Optional database/index for projects larger than the V1 local JSON target.
