# WI-010 Evidence｜Activity recentEvents Default 200

Status: `READY_FOR_REVIEW`

## Execution identity

- Role: R02 Backend Ledger Store fix
- Work item: WI-010
- Owner executionRef: `dce1960e-948a-46ff-81b5-7026190be7cb`
- Independent verifier: R90, pending review

## Scope delivered

The LedgerStore snapshot now exposes the latest 200 valid events by default. The implementation remains a single `slice(-200)` change, so events retain their `events.jsonl` order and ledgers with fewer than 200 events return all available events. No schema, API shape, LKG, or validation flow was changed.

## TDD record

### RED

Added `LedgerStore > keeps the latest 200 events in order and all events when fewer exist` to `tests/ledger/store.test.ts` before changing production code.

```text
pnpm exec vitest run tests/ledger/store.test.ts -t "keeps the latest 200 events in order and all events when fewer exist"
```

The test failed with exit code `1`: expected `recentEvents` length `200`, received `20`. This reproduced the WI-007 finding against the pre-fix implementation.

### GREEN

After changing only `server/ledger/store.ts`:

```text
pnpm exec vitest run tests/ledger/store.test.ts
```

Exit code `0`; all 5 ledger-store tests passed. The regression asserts IDs `bulk-6` through `bulk-205` in order and asserts a one-event ledger returns that event.

## Verification

| Command | Exit | Result |
| --- | ---: | --- |
| `pnpm exec vitest run tests/ledger/store.test.ts` | 0 | 1 file / 5 tests passed |
| `pnpm test` | 0 | 20 files / 69 tests passed |
| `pnpm lint` | 0 | Passed |
| `pnpm build` | 0 | Passed |

The full suite also passed the WI-007 QA tests, including its large-project refresh scenario.

## Review boundary

The change is ready for R90 review. This evidence records neither `VERIFIED` nor `ACCEPTED`; those states remain unset pending independent review.
