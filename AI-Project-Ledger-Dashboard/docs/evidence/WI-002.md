# WI-002 Evidence

Result: READY_FOR_REVIEW

Verification state: NOT VERIFIED

Acceptance state: NOT ACCEPTED

## Execution identity

- Role: R02 Backend Ledger Store / Security
- Work item: WI-002
- Execution reference: `01a0827b-85a8-76f2-a3cb-66a9ff89079f`
- Independent verifier: R90, pending

## Scope implemented

- `server/security/paths.ts` defines the exact seven-file `.ai-ledger` allowlist, sensitive path denylist, and project-root artifact containment.
- `server/ledger/store.ts` reads only the allowlist, validates all seven WI-001 contracts, enforces project root and stable `projectId` binding, builds summary/phase/task-tree data, and retains per-root Last Known Good snapshots.
- Malformed JSON, schema-invalid JSON, cross-file identity mismatches, projectId drift, invalid artifact paths, and invalid events return an `INVALID_LEDGER` warning while preserving the previous valid snapshot.
- No watcher, SSE, API, registry, migration, UI, schema, or package changes were made by this execution.

## TDD evidence

### RED

Command: `pnpm test --run tests/ledger tests/security --reporter=verbose`

- Exit code: 1
- Result: 2 suites / 7 tests failed because the requested `server/ledger/store.ts` and `server/security/paths.ts` modules did not yet exist.

### GREEN

| Command | Exit code | Result |
| --- | ---: | --- |
| `pnpm test --run tests/ledger tests/security` | 0 | 2 suites / 7 tests passed |
| `pnpm exec tsc --noEmit --target ES2022 --module NodeNext --moduleResolution NodeNext --strict --esModuleInterop --skipLibCheck --types node,vitest/globals server/ledger/store.ts server/security/paths.ts tests/ledger/store.test.ts tests/security/security.test.ts` | 0 | WI-002 source and tests type-check |
| `pnpm test:focused` | 0 | 5 suites / 19 tests passed |
| `pnpm test` | 0 | 11 suites / 36 tests passed |
| `pnpm lint` | 0 | TypeScript check passed |
| `pnpm build` | 0 | TypeScript build passed |

## Synthetic/local evidence

- Synthetic fixture source: `fixtures/sample-project/.ai-ledger`; each test copies it to a temporary local project directory and rewrites only the copied `project.json.rootPath`.
- Stable identity: the loaded project remains `cuecut3-example`; the store rejects a later projectId change for the same root and returns the prior snapshot.
- Root binding: the copied project root is normalized and checked against `project.json.rootPath`; sibling and `..` artifact paths are rejected.
- Allowlist: the injected local `readFile` recorder observed exactly `project.json`, `tasks.json`, `roles.json`, `sessions.json`, `artifacts.json`, `events.jsonl`, and `runtime.json`, with no sensitive path read.
- Denylist: `.env`, credentials, token, cookie, password, SSH, and secret path cases are rejected; artifact content is never opened.
- LKG: after replacing copied `tasks.json` with malformed JSON and then with a schema-invalid projectId, the same last valid snapshot is returned with `warning.code === "INVALID_LEDGER"` and `usedLastKnownGood === true`.
- No network access or external service was used by the tests.

## Review boundary

This evidence records R02 implementation and fresh local verification only. R90 must independently review the diff and evidence before any `VERIFIED` or `ACCEPTED` status is assigned.
