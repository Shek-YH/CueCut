# WI-003 Evidence

Result: READY_FOR_REVIEW

Verification state: NOT VERIFIED

Acceptance state: NOT ACCEPTED

## Scope implemented

- `server/atomic.ts` writes to a uniquely named sibling `.tmp` file, calls `sync`, and renames it over the destination.
- `server/watcher/index.ts` uses chokidar with exactly the seven `ALLOWED_LEDGER_FILES` paths, ignores initial events, and debounces changes at 150ms by default. Configured debounce values are restricted to 100–250ms.
- `server/api/index.ts` exposes `GET /api/projects`, `GET /api/projects/:projectId/snapshot`, `GET /api/projects/:projectId/events`, and `GET /api/stream?projectId=...`.
- `server/api/sse.ts` manages reconnectable in-memory SSE clients and emits `ledger:snapshot`, `task:changed`, `event:appended`, `runtime:changed`, and `error` frames.
- `server/index.ts` composes the store, watcher, API, and SSE hub and rejects any bind host other than `127.0.0.1`.
- `chokidar@4.0.0` was added to `package.json` and `pnpm-lock.yaml`.

No database, network service, registry, migration, schema, security, or web code was added or changed by this WI-003 implementation.

## TDD evidence

### RED

Command: `pnpm test --run tests/watcher tests/api --reporter=verbose`

- Exit code: 1
- Result: 4 test files / 5 tests failed because `server/atomic.ts`, `server/watcher/index.ts`, and `server/index.ts` did not yet exist. The failures were the expected `undefined to be defined` feature-missing assertions.

### GREEN

| Command | Exit code | Result |
| --- | ---: | --- |
| `pnpm test --run tests/watcher tests/api --reporter=dot` | 0 | 4 files / 5 tests passed |
| `pnpm exec tsc --noEmit --target ES2022 --module NodeNext --moduleResolution NodeNext --strict --esModuleInterop --skipLibCheck --types node,vitest/globals server/index.ts server/atomic.ts server/api/index.ts server/api/sse.ts server/watcher/index.ts server/ledger/store.ts server/security/paths.ts tests/watcher/atomic.test.ts tests/watcher/watcher.test.ts tests/api/http.test.ts tests/api/sse.test.ts` | 0 | WI-003 server and focused tests type-check with no diagnostics |

## Fresh local verification

All commands below were run locally from `AI-Project-Ledger-Dashboard` after the final source change on 2026-09-08:

| Command | Exit code | Result |
| --- | ---: | --- |
| `pnpm test --run tests/watcher tests/api --reporter=dot` | 0 | 4 files / 5 tests passed |
| `pnpm test:focused` | 0 | Existing focused script: 5 files / 19 tests passed |
| `pnpm test --run tests/schema tests/progress tests/ledger tests/security tests/watcher tests/api --reporter=dot` | 0 | 11 files / 31 tests passed |
| `pnpm test --run --reporter=dot` | 0 | 15 files / 44 tests passed |
| `pnpm exec tsc --noEmit --target ES2022 --module NodeNext --moduleResolution NodeNext --strict --esModuleInterop --skipLibCheck --types node,vitest/globals server/index.ts server/atomic.ts server/api/index.ts server/api/sse.ts server/watcher/index.ts server/ledger/store.ts server/security/paths.ts tests/watcher/atomic.test.ts tests/watcher/watcher.test.ts tests/api/http.test.ts tests/api/sse.test.ts` | 0 | No diagnostics |
| `pnpm lint` | 0 | Existing TypeScript lint command passed |
| `pnpm build` | 0 | Existing TypeScript build command passed |

## Synthetic/local integration evidence

- The watcher tests create real temporary project folders from `fixtures/sample-project`, wait for chokidar readiness, verify all seven watched paths are under `<root>/.ai-ledger`, and observe one callback after two writes within a 150ms debounce window.
- The atomic writer test reads the destination after the operation and confirms no temporary file remains.
- The HTTP test starts a real Node HTTP server on an ephemeral port, confirms the bound host is `127.0.0.1`, and reads projects, aggregate snapshot, and event-log routes over HTTP.
- The SSE test opens a real HTTP event stream, edits `tasks.json`, confirms `ledger:snapshot` and `task:changed`, then writes malformed JSON and confirms `error` with `usedLastKnownGood: true`.
- Temporary test project folders are removed by test cleanup. No external network or service is used, and no secret file contents are read.

## Review boundary

This evidence records the R02 implementation and fresh local verification only. R90 must independently review the diff and evidence before any `VERIFIED` or `ACCEPTED` status is assigned.
