# WI-005 Evidence

Result: READY_FOR_REVIEW

Verification state: NOT VERIFIED

Acceptance state: NOT ACCEPTED

## Execution identity

- Role: R03 Dashboard UX / React
- Work item: WI-005
- Fix executionRef: `01a082e8-cfcc-7c80-ba91-41876b3b0b9a`
- Scope: `web/**`, `tests/ui/**`, `tests/e2e/**`, and WI-005 evidence/handoff docs
- Server, schema, `.ai-ledger`, and global Skill files were not modified by this R03 execution.

## R03 fix execution

The R90 recheck findings were reproduced and fixed. `DashboardApp` now shares an in-flight aggregate snapshot request by `projectId`, so the current React `StrictMode` mount path issues one snapshot request; the last consumer's cleanup aborts the shared request after the StrictMode replay window. The Artifacts action now calls WI-009's `POST /api/projects/:projectId/artifacts/open` endpoint with `{ path: artifact.path }`, and reports opening, success, and backend error states. The browser remains read-only for task state; project-root validation and the platform opener stay in the backend.

### TDD fix evidence

| Stage | Command | Exit code | Result |
| --- | --- | ---: | --- |
| RED | `pnpm test --run tests/ui/dashboard.test.ts --reporter=verbose` | 1 | StrictMode regression failed as expected: the same project snapshot loader was called 2 times instead of 1. |
| RED | `pnpm test --run tests/ui/dashboard.test.ts --reporter=verbose` | 1 | Artifact regressions failed as expected: the no-op action made 0 backend calls and displayed no success/error state. |
| GREEN | `pnpm test --run tests/ui` | 0 | 2 files / 9 tests passed, including StrictMode cancellation and artifact success/error interaction coverage. |

### Fresh fix verification

All commands below were rerun after the fix from `F:\CCPJ\CueCut3\AI-Project-Ledger-Dashboard` on 2026-09-08:

| Command | Exit code | Result |
| --- | ---: | --- |
| `pnpm test:focused -- --reporter=dot` | 0 | 7 files / 28 tests passed |
| `pnpm test --run tests/ui` | 0 | 2 files / 9 tests passed |
| `pnpm test` | 0 | 19 files / 61 tests passed |
| `pnpm lint` | 0 | TypeScript check passed |
| `pnpm build` | 0 | TypeScript build and Vite production build passed |
| `pnpm test:e2e` | 0 | 1 browser test passed; Chromium launched successfully; E2E observed one aggregate snapshot request and validated the artifact-open POST body |

This is an owner fix handoff for independent re-review. It does not mark WI-005 `VERIFIED` or `ACCEPTED`.

## Scope implemented

- Added a Vite React dashboard with Overview, Tasks, Blockers, Activity, Roles, Artifacts and Settings views.
- Added one aggregate snapshot client for `/api/projects/:projectId/snapshot` and reconnectable `EventSource` consumption of `ledger:snapshot` plus structured `error` frames.
- Tasks supports phase grouping, nested tree rendering, status/role/priority filters and search.
- BLOCKED and WAITING_USER are rendered in separate regions; every status includes visible text, a status icon and an accessible label.
- Task cards use a 4px fixed-token status bar and status-colored progress bars.
- Settings exposes registry/watcher controls only; task status/progress/role/session state has no mutation UI and is explicitly read-only.
- Artifacts render validated relative paths with Copy Path/Open Parent Folder controls; the open action sends only `projectId` plus the relative `artifact.path` to WI-009, and no project file is read by the browser.

## Original implementation TDD evidence (historical)

### RED

| Command | Exit code | Evidence |
| --- | ---: | --- |
| `pnpm test --run tests/ui --reporter=verbose` | 1 | Both new UI suites failed during collection because `web/App.js` and `web/data.js` did not exist. |
| `pnpm test --run tests/ui/data.test.ts --reporter=verbose` | 1 | The SSE regression test failed with `Ledger stream disconnected` instead of the structured `Invalid schema in tasks.json` message. |

### GREEN

| Command | Exit code | Evidence |
| --- | ---: | --- |
| `pnpm test --run tests/ui --reporter=dot` | 0 | 2 UI suites / 5 tests passed. |
| `pnpm test --run tests/ui/data.test.ts --reporter=dot` | 0 | Aggregate snapshot request and structured SSE error handling passed. |

## Initial implementation verification (historical)

The table below records the pre-R90 implementation state and is superseded by the fresh fix verification above.

All commands below were rerun after the final source/package changes on 2026-09-08:

| Command | Exit code | Result |
| --- | ---: | --- |
| `pnpm test:focused -- --reporter=dot` | 0 | 7 files / 25 tests passed. |
| `pnpm test -- --reporter=dot` | 0 | 18 files / 52 tests passed. |
| `pnpm lint` | 0 | TypeScript check passed with `--jsx react`. |
| `pnpm build` | 0 | TypeScript build and Vite production build passed. |
| `pnpm test:e2e` | 1 | Test runner started Vite, but Playwright Chromium is not installed in this environment; no browser assertion ran. |

## Initial implementation browser smoke evidence

- `pnpm dev:all` started the real local Ledger API and Vite dashboard.
- Local Chrome loaded `http://127.0.0.1:4173/?projectId=ai-project-ledger-dashboard-v1` from the real `.ai-ledger` aggregate snapshot.
- Overview rendered the project identity/progress/runtime state; Tasks navigation rendered 10 leaf tasks, filters, fixed status colors and `READ-ONLY`.
- A Tasks-page screenshot was captured in the review run and emitted to the task transcript.

## Review boundary

This is implementation and fresh local verification evidence for independent review. It is not a verification or acceptance decision. R90 must independently re-review the current diff and may decide `VERIFIED`/`ACCEPTED` separately.
