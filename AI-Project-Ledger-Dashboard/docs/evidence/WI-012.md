# WI-012 Evidence｜Settings Add Project Folder UI

Status: `READY_FOR_REVIEW`

Verification state: `NOT VERIFIED`

Acceptance state: `NOT ACCEPTED`

## Execution identity

- Role: R03 Dashboard UX / React
- Work item: WI-012
- Owner executionRef: `01a08389-6249-7d33-9df4-14e415442539`
- Dependencies checked: WI-011 verifier `PASS`; WI-005 final UI verifier `PASS`
- Scope: `web/**`, `tests/ui/**`, `tests/e2e/**`, and WI-012 context/handoff/evidence docs
- Server, schema, `.ai-ledger`, and global Skill files were not modified by this execution.

## Delivered behavior

| Requirement | Evidence |
| --- | --- |
| Accessible project-root input | Settings renders the labeled `Project root folder` text input with `aria-describedby` help text and a submit button. |
| WI-011 POST contract | `web/data.ts` sends `POST /api/projects` with JSON body exactly `{ rootPath }`, parses `201` `{ ok, kind, project, warnings, legacyFiles }`, and surfaces stable `error` messages. |
| Bootstrap feedback | Settings reports `Project registered` and distinguishes `Existing ledger registered`, `Legacy Markdown migrated`, and `Empty ledger initialized`; sanitized warning/file counts are shown when present. |
| Refresh and switch | After a successful add, the app reloads the project list, selects the returned stable `projectId`, loads the aggregate snapshot, and reconnects the existing project SSE lifecycle. |
| Failure path | Settings renders backend/API failures as an accessible `role="alert"` without writing project files. |
| Read-only boundary | The new surface only accepts a local root path. No task status, progress, role, or session write input/control was added; existing `READ-ONLY` and task-state notice remain. |

## TDD evidence

### RED

| Command | Exit | Result |
| --- | ---: | --- |
| `pnpm test --run tests/ui --reporter=verbose` | 1 | 2 data tests failed because `addProject` was absent; 2 Settings tests failed because the root input/flow was absent; 9 existing UI/data tests passed. |
| `pnpm test:e2e -- --grep "adds a real project folder"` | 1 | The new browser test failed at the missing Settings input; no real add assertion could pass before implementation. |

### GREEN

| Command | Exit | Result |
| --- | ---: | --- |
| `pnpm test --run tests/ui --reporter=verbose` | 0 | 2 UI files / 13 tests passed. |
| `pnpm test:e2e -- --grep "adds a real project folder"` | 0 | Real Vite + Ledger server started; 2/2 browser tests passed. |

## Fresh verification evidence

All commands were run from `F:\CCPJ\CueCut3\AI-Project-Ledger-Dashboard` on 2026-09-08:

| Command | Exit | Result |
| --- | ---: | --- |
| `pnpm test:focused -- --reporter=dot` | 0 | 7 files / 32 tests passed. |
| `pnpm test -- --reporter=dot` | 0 | 21 files / 79 tests passed. |
| `pnpm lint` | 0 | TypeScript no-emit check passed. |
| `pnpm build` | 0 | TypeScript build passed; Vite transformed 26 modules. |
| `pnpm test:e2e` | 0 | Real Vite + localhost Ledger server; 2/2 Chromium tests passed. |

## Real bounded browser evidence

- `tests/e2e/playwright.config.ts` starts `pnpm dev:all`, not Vite alone, with an isolated temporary `APPDATA` registry and the dashboard project as the initial root.
- The add-project test creates a temporary empty directory outside the repository and does not route/mock project API calls.
- The browser observed a real `POST /api/projects` response `201` with `kind=initialized` and the resolved temporary root, then a real `GET /api/projects` refresh and new-project snapshot request.
- The test removed its temporary fixture in `finally`; the server initialized the normal WI-011 ledger skeleton and registered the project during the test.

## Review boundary

This is an owner implementation handoff. R90 should independently review the current diff and real GP-01 path. This document intentionally does not assign `VERIFIED` or `ACCEPTED`.
