# WI-006 Evidence — Skill Integration / Dual Write

**Status:** `READY_FOR_REVIEW`  
**Role owner:** R05  
**Verifier:** R90 (pending independent review)  
**Dependency:** WI-001 schema contract

## Scope

Upgrade the existing global `AI_Autonomous_Project_Ledger_Skill` with an enforceable machine-ledger dual-write contract. Agent Board integration and Dashboard production code are out of scope.

## RED evidence

- Scenario: [`docs/skill-integration/WI-006_PRESSURE_SCENARIO.md`](../skill-integration/WI-006_PRESSURE_SCENARIO.md)
- Baseline result: [`docs/skill-integration/WI-006_BASELINE_RESULT.md`](../skill-integration/WI-006_BASELINE_RESULT.md)
- RED contract test: `pnpm exec vitest run tests/skill/wi-006-contract.test.ts` failed on the missing seven-file/dual-write contract; lifecycle-gate assertions remained green.
- Baseline child-agent run was read-only and exposed ambiguity around initialization, root binding, status invariants, atomic recovery, LKG replacement, secret enumeration, and Dashboard task writes.

## GREEN evidence

- Updated result: [`docs/skill-integration/WI-006_UPDATED_RESULT.md`](../skill-integration/WI-006_UPDATED_RESULT.md)
- GREEN contract test: `pnpm exec vitest run tests/skill/wi-006-contract.test.ts` passed (2 tests).
- Same pressure scenario with updated Skill produced compliant decisions for all requested contract areas and did not write project files.

## Implemented contract

The global Skill now states:

- exactly seven initialization files, with `git.json` explicitly optional Git metadata;
- stable `projectId` bound to normalized Project Root, with mismatch rejection and Session ID separation;
- the seven allowed statuses and progress/reason/completion invariants;
- required dual-write order: `tasks.json` → `project.json.updatedAt` → `runtime.json.lastWriteAt` → `events.jsonl` → Markdown;
- sibling temp write followed by complete write and rename;
- append-only JSONL with no rewrite/delete/reorder/truncate cleanup;
- schema/binding/invariant/event validation before replacing Last Known Good;
- explicit prohibition on `Password, API Key, Token, Cookie, Secret` values;
- Dashboard V1 read-only project task state;
- additive preservation of multi-agent, Golden Path, independent verifier, Git, and security gates, with no Agent Board integration.

## Changed paths

- `C:\Users\Administrator\.codex\skills\ai-autonomous-project-ledger-skill\SKILL.md`
- `docs/skill-integration/**`
- `tests/skill/wi-006-contract.test.ts`
- `docs/evidence/WI-006.md`
- `docs/handoffs/WI-006.md`

No `server/`, `web/`, `package.json`, `.ai-ledger` task data, or Dashboard source was modified. No password, API key, token, cookie, or secret value was written.

## Review gate

This evidence is `READY_FOR_REVIEW`, not `VERIFIED` or `ACCEPTED`. R90 should independently inspect the global Skill diff, rerun the pressure scenario, confirm the seven-file wording against the Runtime Contract, and confirm that existing governance gates remain intact.

