# WI-008 Evidence — Core Golden Path / Final Acceptance Preparation

Status: `WAITING_USER`

## Real local path

- `GP-01`: the real Settings form accepted a temporary local directory and POSTed `{rootPath}` to `/api/projects`; the bounded Chromium test received `201 kind=initialized`, refreshed the registry/list and loaded the new snapshot, then cleaned the temporary project.
- `GP-02`: `/api/projects` identified `ai-project-ledger-dashboard-v1` from this project's `.ai-ledger/project.json`.
- `GP-03`: the real seven-file ledger was validated and aggregated into one snapshot.
- `GP-04`: Chromium rendered Overview, Tasks, Blockers, Activity, Roles, Artifacts and Settings.
- `GP-05`: atomic task update and watcher/SSE changed `T-004` from IN_PROGRESS to COMPLETED in 214.13ms.
- `GP-06`: malformed tasks JSON retained LKG, displayed the warning, then recovered after valid JSON restoration.
- `GP-07`: Legacy Migration has independent R90 PASS and preserves Markdown.
- `GP-08`: this Dashboard loaded and observed its own `.ai-ledger`; no Agent Board was used.

## Screenshots

- `docs/evidence/screenshots/overview.png` — real self-project Overview.
- `docs/evidence/screenshots/tasks.png` — real self-project Tasks tree with parent/child cards and WAITING_USER final task.
- `docs/evidence/screenshots/settings.png` — final Settings view with Add Project Folder input and explicit read-only boundary.

## Acceptance gate

All implementation/QA Work Items, including WI-011/WI-012 Add Project, have independent R90 PASS and are accepted by R00. The only remaining gate is a single user Product/Release Acceptance view of the local URL. No production deploy, account, secret, external API or Agent Board integration is part of V1.
