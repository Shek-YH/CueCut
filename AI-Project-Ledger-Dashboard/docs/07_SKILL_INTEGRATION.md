# Skill Integration Notes

`AI_Autonomous_Project_Ledger_Skill_v1` remains the orchestration source of truth and is extended to require `.ai-ledger` initialization plus Markdown + JSON/JSONL dual write. The Dashboard does not integrate Agent Board in V1. Any Skill update must retain real child-agent/verifier gates and secret restrictions.
